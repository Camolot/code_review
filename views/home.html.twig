<html>
    <head>
        <title>Contact List</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
    </head>
    <body>
        <h1>Here is a list of all of your contacts.</h1>

        {% if contacts is not empty %}
            <ul>
                {% for contact in contacts %}
                    <li>{{ contact.getContact }}</li>
                {% endfor %}
            </ul>
        {% else %}
        <h2>Currently you have no contacts! Please use the Add Contacts button to add some!</h2>
        {% endif %}

        <form action='/add_contact'>
            <button type='submit' class='btn btn-default'>ADD CONTACT</button>
        </form>
        <form action='/delete_contact'>

      </div>
    </body>
</html>
